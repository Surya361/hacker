#include<stdio.h>
#include<stdlib.h>

int main()
{
	char *in = malloc(100000*sizeof(char));
	scanf("%s",in);
	int flag = check(in);
}

int check(char *in)
{
	int *alpha = malloc(26*sizeof(int));
	
}

